# `@agentsy/memory`

Cognitive memory engine with tiered event storage, synthesized wiki, RAG retrieval, sync, and MCP surfaces.

## Boundary rule

This package has a legitimate operator-facing surface because it ships a CLI/MCP/server shape. It may document those package-local operational details. Framework-wide `setup` and `doctor` UX still belongs to `@agentsy/cli`.
