# @agentsy/context

Token accounting and token-budget utilities.

## Status

Pre-release package with evolving optimization strategies.

## Phase 0 foundation APIs

- `compressConversation(...)`
- `compressOutput(...)` (levels: `lite`, `full`, `ultra`)
- `createInMemoryTokenManager()`
- `PacingController`
