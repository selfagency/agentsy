# This file has been renamed → plan/LLM-GATEWAY-PLAN.md

This plan has been renamed. The full content with all references updated is now at `plan/LLM-GATEWAY-PLAN.md`.

---

The LLM Gateway plan implements the same multi-provider routing design as the original load balancer concept, but with an expanded scope: semantic and capability-aware routing that considers model capability metadata (supported tools, context window, modality) in addition to latency and cost. All references have been updated from "Load Balancer" / "load-balancer" to "LLM Gateway" / "llm-gateway".
