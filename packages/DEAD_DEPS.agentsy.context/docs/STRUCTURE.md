# Structure

Public API barrels live in `src/public/*`, with subsystem implementations under `src/compression`, `src/drift`, `src/compaction`, `src/observability`, `src/retrieval`, and `src/strategies`.
